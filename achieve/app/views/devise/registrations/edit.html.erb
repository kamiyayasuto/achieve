<div class="container user_edit_setting">
  <div class="row">
    <h2>マイページ</h2>
  </div>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <% if devise_error_messages!.present? %>
    <div class="row">
      <div class="alert alert-danger " role="alert">
         <span class=""><%= devise_error_messages! %></span>
      </div>
    </div>
  <% end %>
  <div class="row">
    <div class="col-xs-3 col-sm-3 title"><%= f.label :メールアドレス %></div>
    <div class="col-xs-9 col-sm-9 content"><%= f.email_field :email, autofocus: true, class: "form-control" %></div>
  </div>
  <div class="row">
    <div class="col-xs-3 col-sm-3 title"><%= f.label :名前 %></div>
    <div class="col-xs-9 col-sm-9 content"><%= f.text_field :name, class: "form-control" %></div>
  </div>
  <% if @user.provider.blank? %>
  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
  <div class="alert alert-warning" role="alert">Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>
   <div class="row">
    <div class="col-xs-3 col-sm-3 title"><%= f.label :password %></div>
    <div class="col-xs-9 col-sm-9 content"><%= f.password_field :password, autocomplete: "off", class: "form-control"  %></div>
    <div class="col-xs-offset-3 col-xs-9 info">変更したい場合だけ入力してください(<%= @minimum_password_length %>文字以上)</div>
  </div>
   <div class="row">
    <div class="col-xs-3 col-sm-3 title"><%= f.label :password_confirmation %></div>
    <div class="col-xs-9 col-sm-9 content"><%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %></div>
    <div class="col-xs-offset-3 col-xs-9 info">新しいパスワードをもう一度入力してください。</div>
  </div>
   <div class="row">
    <div class="col-xs-3 col-sm-3 title"><%= f.label :current_password %></div>
    <div class="col-xs-9 col-sm-9 content"><%= f.password_field :current_password, autocomplete: "off", class: "form-control" %></div>
    <div class="col-xs-offset-3 col-xs-9 info">変更確定のため、現在のパスワードを入力してください。</div>
  </div>
  <% end %>
  <div class="row">
    <div class="col-xs-3 col-sm-3 title">イメージ画像</div>
    <div class="col-xs-9 col-sm-9 content">
      <%= profile_img(@user) if profile_img(@user) %>
      <%= f.file_field :avatar%>
      <%= f.hidden_field :avatar_cache %>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 actions">
      <%= link_to "TOPへ戻る", :back, class:'btn btn-default btn_size' %>
      <%= f.submit "更新する", class:'btn btn-primary btn_size'%>
    </div>
  </div>
  <% end %>
  <div class="row">
    <h3>このアカウントを削除する場合はこのボタンをクリック！</h3>
    <p><%= button_to "アカウントの削除", registration_path(resource_name), data: { confirm: "本当に削除してもよろしいですか？?" }, method: :delete, class:'btn btn-danger btn_size' %></p>
  </div>
</div>
